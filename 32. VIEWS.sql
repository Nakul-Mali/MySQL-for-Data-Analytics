
/* VIEW is not a physical table, it is a virtual table created by a query joining one or more tables*/

-- syntax
/*
CREATE [OR REPLACE] VIEW view_name AS
SELECT columns
FROM tables
[WHERE conditions];
*/

-- view is a query it is not a table but it behave like a table
-- Whenever you look at a view it will contain containts as a table but it'll actully a query


